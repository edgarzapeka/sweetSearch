export class Image{
    url: string;
    author: string;
    resource: string;

    constructor(url, author, resource){
        this.url = url;
        this.author = author;
        this.resource = resource;
    }
}